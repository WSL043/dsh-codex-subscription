# Additive community bundle: it contributes one trusted provider route and its
# settings surface. It never replaces DSH's default model or fallback policy.
- insert:
    - id: wsl043-codex-boundary
      name: '@wsl043/dsh-codex-subscription/boundary'
    - id: wsl043-codex-subscription
      name: '@wsl043/dsh-codex-subscription'
