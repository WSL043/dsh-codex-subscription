name: Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release automation is temporarily paused while v1.0.2 is prepared."
